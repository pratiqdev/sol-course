import Editor from '@components/Editor'
import Shell from '@components/Shell'
import Instructions from '@components/Instructions'

<Shell>
<Instructions>

# MDX Content

This page uses basic mdx

> convert the `ProtectedRoute` HOC to a useEffect inside the `Shell`
> that is only triggered if the `courseList` has `restricted: true`

<code>
codeblock!
</code>

</Instructions>

<Editor 
code={`// custom code defined in 'straight-mdx'`} 
tests={[
    {test1: 'a test'}
]}
/>
</Shell>